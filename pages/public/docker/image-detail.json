{
  "type": "page",
  "title": "${params.image}",
  "remark": null,
  "initApi": {
    "url": "${API_HOST}/image/detail",
    "data": {
      "image": "${params.image}"
    },
    "method": "get",
    "adaptor": "return {\n    ...payload,\n    status: payload.status === 200 ? 0 : payload.status,\n    msg: payload.message,\n    data: {response: payload.data}\n}"
  },
  "toolbar": [
    {
      "type": "button",
      "actionType": "link",
      "link": "/application/inventory",
      "label": "返回应用与镜像仓库",
      "level": "primary",
      "size": "lg"
    }
  ],
  "body": [
    {
      "type": "control",
      "name": "response",
      "body": {
        "type": "json",
        "jsonTheme": "eighties",
        "levelExpand": 100
      }
    }
  ]
}
