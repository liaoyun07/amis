{
  "type": "page",
  "title": "${params.namespace}/${params.name}",
  "remark": null,
  "initApi": {
    "url": "${API_HOST}/service/search/single",
    "data": {
      "serviceName": "${params.name}",
      "namespaceName": "${params.namespace}"
    },
    "method": "get",
    "adaptor": "return {\n    ...payload,\n    status: payload.status === 200 ? 0 : payload.status,\n    msg: payload.message,\n    data: {response: payload.data}\n}"
  },
  "toolbar": [
    {
      "type": "button",
      "actionType": "link",
      "link": "/service",
      "label": "返回Service列表",
      "level": "primary",
      "size": "lg"
    }
  ],
  "body": [
    {
      "type": "control",
      "name": "response",
      "body": {
        "type": "json",
        "jsonTheme": "eighties",
        "levelExpand": 100
      }
    }
  ]
}