# Frontend Deployment Notes

This repository contains the AMIS frontend.

## Start

```powershell
npm install
npm start
```

Default URL:

```text
http://127.0.0.1:3000
```

Use `.env.example` as the reference for the backend gateway address.
